# 输入直角三角形的宽度和高度，输出面积
def main():
    a = float(input('Enter the wide of triangle: '))
    b = float(input('Enter the high of triangle: '))
    area = (a * b) / 2
    print(f'The area of the triangle is {area}')


main()
